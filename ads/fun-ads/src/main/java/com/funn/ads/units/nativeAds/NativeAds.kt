package com.funn.ads.units.nativeAds

import android.app.Activity
import androidx.lifecycle.Lifecycle
import com.monetization.adsmain.commons.sdkNativeAdd
import com.monetization.adsmain.widgets.AdsUiWidget
import com.monetization.core.ui.LayoutInfo

fun AdsUiWidget.register(
    activity: Activity,
    lifecycle: Lifecycle,
    placementKey: String
) {

    /*sdkNativeAdd(
        adLayout = LayoutInfo.LayoutByName(""),
        adKey = ,
        placementKey = ,
        showShimmerLayout = ,
        requestNewOnShow = ,
        lifecycle = lifecycle,
        activity = activity,
    )*/
}